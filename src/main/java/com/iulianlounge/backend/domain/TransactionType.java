package com.iulianlounge.backend.domain;

// Por qué se movieron fichas. Añadir un tipo exige ampliar el CHECK de token_transaction.type (V4)
public enum TransactionType {
    // Las 100 fichas con las que nace cada cuenta
    WELCOME_BONUS
}
